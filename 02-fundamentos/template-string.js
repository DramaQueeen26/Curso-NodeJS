const nombre = 'Deadpool';
const real = 'Wade Winston';

const normal = nombre + ' ' + real;

const template = `${nombre} ${real}` //Backticks o como se escriba xd

console.log(template); 

const html = `
    <h1>Hola</h1>
    <p>Mundo</p>
`; //Saltos de línea

console.log(html);
